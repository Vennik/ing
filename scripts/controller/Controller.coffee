define [
  'controller/ZIndexController'
], (ZIndexController) ->
  class Controller
    # Controls the zIndex
    # Controls connections?
    # Controls everything?
    # Might not need this

    new ZIndexController()